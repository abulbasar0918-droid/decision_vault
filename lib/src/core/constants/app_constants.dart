/// Application-level constants used across the Decision Vault app.
class AppConstants {
  AppConstants._();

  static const String appName = 'Decision Vault';
  static const String appDescription =
      'A premium offline-first decision support application.';
}
